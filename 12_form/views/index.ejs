<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <p><%= desc %></p>
    <h2>GET 요청</h2>
    <form action="/getForm" name="login1" method="get">
      <input type="text" name="id" placeholder="ID" required minlength="3" />
      <input
        type="password"
        name="pw"
        placeholder="PW"
        required
        minlength="4"
      />
      <input type="email" name="id" placeholder="ID" required />
      <input type="number" placeholder="grade" required min="3" max="7" />
      <button type="submit">제출</button>
    </form>

    <h2>POST 요청</h2>
    <form action="/postForm" name="login2" method="post">
      <!-- ID조건: 대소문자, 한글, 숫자, 4자 이상 -->
      <!-- PW조건: 소문자, 숫자, 8자 이상 12자 이하 -->
      <input
        type="text"
        name="id"
        placeholder="ID"
        required
        pattern="^([a-zA-Z0-9가-힣]){4,}$"
        title="ID는 대소문자, 한글, 숫자로 4자 이상해주세요"
      />
      <input
        type="password"
        name="pw"
        placeholder="PW"
        required
        pattern="^([a-z0-9]){8,12}$"
        title="PW는 소문자, 한글, 숫자로 8자 이상 12자 이하로 해주세요"
      />
      <button type="submit">제출</button>
    </form>

    <h2>GET요청2</h2>
    <form action="/getForm2" name="email1" method="get">
      <input type="email" name="id" placeholder="ID" required />
      <input type="password" name="pw" placeholder="PW" />
      <button type="submit">로그인</button>
    </form>

    <h2>POST요청2</h2>
    <form action="/postForm2" name="email2" method="post">
      <input type="email" name="id" placeholder="ID" />
      <input type="password" name="pw" placeholder="PW" />
      <button type="submit">로그인</button>
    </form>

    <h2>login 창</h2>
    <form action="/postForm3" name="loginEvent" method="post">
      <fieldset>
        <legend>로그인 이벤트</legend>
        <label for="userId">userId</label>
        <input type="email" name="userId" id="userId" />
        <label for="userPw">userPw</label>
        <input type="password" name="userPw" id="userPw" />
        <button type="submit">로그인</button>
      </fieldset>
    </form>
    <h3><%= fin %></h3>
    <script>
      // form 태그의 name속성 값으로 폼 선택하기
      console.log(document.forms["login1"]);
      console.log(document.forms["login2"]);
    </script>
  </body>
</html>
